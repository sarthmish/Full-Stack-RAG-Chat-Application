name: rag_application
channels:
  - defaults
dependencies:
  - bzip2=1.0.8=h2bbff1b_6
  - ca-certificates=2024.3.11=h56e8100_0 # Use a standard, public ca-certificates version
  - libffi=3.4.4=hd77b12b_1
  - openssl=3.0.13=h2bbff1b_0 # Use a standard, public openssl version
  - pip=24.0=pyhd8ed1ab_0 # Use a standard, public pip version
  - python=3.11.8=h2628c6u_0 # Use a standard, public Python 3.11 version (update to current stable if desired)
  - sqlite=3.45.3=h2bbff1b_0
  - tk=8.6.13=h0416ee5_0 # Use a standard, public tk version
  - vc=14.3=h3d0cd1c_1 # Standard MSVC runtime for Windows if still needed
  - vs2015_runtime=14.38.33130=h59f6356_0 # Standard MSVC runtime for Windows if still needed
  - wheel=0.42.0=py311haa95532_0 # Use a standard, public wheel version
  - xz=5.4.6=h8cc25b3_0 # Use a standard, public xz version
  - zlib=1.2.13=h8cc25b3_1
prefix: ./envs/rag_application # Change prefix to a relative path for public use